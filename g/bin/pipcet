#!/bin/bash
git submodule foreach --recursive git config user.name 'Pip Cet'
git submodule foreach --recursive git config user.email 'pipcet@gmail.com'
git submodule foreach --recursive git config pull.rebase false
