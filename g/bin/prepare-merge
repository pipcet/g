#!/bin/bash
while ! [ -d .git ]; do
    cd ..
done
rm -rf tmp
WD=$(pwd)
cat g/merge-branches.txt | while read REPO BRANCH; do
    (cd $REPO && TMPDIR=$WD/tmp BRANCH=$BRANCH $WD/g/bin/prepare-specific-merge)
done
